# Node 20 richiesto da Next.js 16 e Tailwind
[variables]
NIXPACKS_NODE_VERSION = "20"

# Usa npm install invece di npm ci così package.json ha priorità (pdf-parse@1.1.1)
[phases.install]
cmds = ["npm install"]
